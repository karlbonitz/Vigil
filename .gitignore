# packager output / OS noise
*.zip
.DS_Store
Thumbs.db

# packager local dry-run output
.release/

# Vantage has no libraries (see .pkgmeta). Kept so a stray Libs/ from an old
# checkout can never sneak into a commit.
Libs/
